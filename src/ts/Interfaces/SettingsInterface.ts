import PropsInterface from "./PropsInterface";
import {BlockConfiguration} from "@wordpress/blocks";

export default interface SettingsInterface extends BlockConfiguration {

	// title: string,
	// description: any,
	// icon: {
	// 	background: string,
	// 	foreground: string,
	// 	src: string
	// },
	// keywords: any[],
	// supports: {
	// 	anchor: boolean,
	// 	align: string[],
	// 	color: {
	// 		gradients: boolean,
	// 		link: boolean
	// 	},
	// 	spacing: {}
	// },
	// example: {},
	// edit: (props: PropsInterface) => JSX.Element,
	// save: (props: PropsInterface) => JSX.Element
}
